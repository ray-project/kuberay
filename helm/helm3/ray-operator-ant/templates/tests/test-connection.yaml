apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ray-operator-ant.fullname" . }}-test-connection"
  labels:
{{ include "ray-operator-ant.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ray-operator-ant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
